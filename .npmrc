# Required for Expo SDK 54 + Metro to resolve dependencies that
# assume a flat node_modules layout.
node-linker=hoisted

# Allow peer deps that Expo / RN don't always declare explicitly.
strict-peer-dependencies=false
auto-install-peers=true
